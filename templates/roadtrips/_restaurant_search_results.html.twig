<div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 min-h-[300px]" id="restaurant-results">
	{% for restaurant in restaurants %}
		{{ include('restaurant/_card.html.twig', { restaurant: restaurant }) }}
	{% else %}
		<p class="text-center col-span-full">Aucun restaurant trouvé</p>
	{% endfor %}
</div>