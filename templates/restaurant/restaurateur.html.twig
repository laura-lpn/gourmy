{% extends 'base.html.twig' %}

{% block title %}Espace Restaurateur
{% endblock %}

{% block meta %}
	<meta name="description" content="Rejoignez Gourmy et valorisez votre cuisine locale. Augmentez votre visibilité sans commission et attirez des gourmets passionnés.">
	<meta property="og:title" content="Espace Restaurateur – Devenez partenaire Gourmy">
	<meta property="og:description" content="Intégrez une plateforme dédiée aux restaurants locaux. Profitez d’une visibilité ciblée et d’outils marketing, sans commission.">
{% endblock %}

{% block body %}
	<section class="mt-28 pb-16 max-w-7xl w-11/12 md:w-5/6 lg:w-4/5 mx-auto px-4 sm:px-6 lg:px-8">
		<h1 class="text-2xl sm:text-3xl lg:text-4xl leading-tight font-second text-blue text-center mb-4 sm:mb-6">
			Faites rayonner votre cuisine sur Gourmy
		</h1>

		<p class="text-sm sm:text-base md:text-lg text-center mb-8 max-w-3xl mx-auto">
			Rejoignez Gourmy et intégrez une plateforme pensée pour valoriser les restaurateurs locaux et passionnés. Profitez d’une visibilité ciblée, sans commission, dans un univers
			<em class="text-blue not-italic">100% terroir.</em>
		</p>

		<div class="text-center">
			<a href="{{ path('app_restaurant_create') }}" class="btn inline-block text-base sm:text-lg px-6 sm:px-8 py-2.5 sm:py-3">
				Créer mon restaurant
			</a>
		</div>

		<h2 class="text-lg sm:text-xl font-medium flex items-center gap-4 sm:gap-6 font-second text-orange mt-10 sm:mt-12 mb-4 sm:mb-6">
			<span class="h-0.5 rounded-md bg-orange w-6 sm:w-8 block"></span>
			Pourquoi devenir partenaire Gourmy
		</h2>

		<div class="grid grid-cols-1 sm:grid-cols-2 gap-4 sm:gap-6 mb-12 sm:mb-16">
			{% set avantages = [
        { icon: 'fa-bullseye', color: 'blue', title: 'Visibilité ciblée', text: 'Votre restaurant est intégré dans des road-trips gourmands personnalisés selon les préférences des utilisateurs.' },
        { icon: 'fa-euro-sign', color: 'blue', title: 'Sans commission', text: 'Un abonnement fixe, pas de frais par réservation. Vos revenus vous reviennent en totalité.' },
        { icon: 'fa-chart-line', color: 'blue', title: 'Statistiques de performance', text: 'Analysez les visites, les clics et l’engagement autour de votre établissement.' },
        { icon: 'fa-bullhorn', color: 'blue', title: 'Outils marketing intégrés', text: 'Ajoutez vos offres, menus du jour et gérez votre e-réputation directement sur la plateforme.' }
      ] %}
			{% for avantage in avantages %}
				<article class="bg-{{ avantage.color }}/5 rounded-xl p-5 sm:p-6 border border-{{ avantage.color }}/10 h-full flex flex-col">
					<h3 class="text-{{ avantage.color }} font-second font-medium text-base sm:text-lg mb-2 flex items-center">
						<i class="fa-solid {{ avantage.icon }} mr-2" aria-hidden="true"></i>
						<span>{{ avantage.title }}</span>
					</h3>
					<p class="text-sm leading-relaxed">{{ avantage.text }}</p>
				</article>
			{% endfor %}
		</div>

		<h2 class="text-lg sm:text-xl font-medium flex items-center gap-4 sm:gap-6 font-second text-orange mb-4 sm:mb-6">
			<span class="h-0.5 rounded-md bg-orange w-6 sm:w-8 block"></span>
			Nos formules d’abonnement
		</h2>

		<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6 mb-12 sm:mb-16">
			{% set plans = [
        {
          name: 'Freemium',
          color: 'blue',
          price: 'Gratuit',
          features: ['Fiche restaurant basique', 'Apparition dans les road-trips', 'Accès aux avis'],
          badge: false
        },
        {
          name: 'Gourmy Premium',
          color: 'orange',
          price: '26,99€/mois',
          features: ['Fiche mise en avant', 'Badge “Partenaire Premium”', 'Statistiques détaillées', 'Offres spéciales & menus du jour'],
          badge: true
        },
        {
          name: 'Gourmy Premium +',
          color: 'blue',
          price: '39,99€/mois',
          features: ['Toutes les options Premium', 'Réservations en ligne', 'Campagnes marketing ciblées', 'Données clients avancées'],
          badge: true
        }
      ] %}
			{% for plan in plans %}
				<article class="relative bg-{{ plan.color }}/5 rounded-xl p-5 sm:p-6 text-sm border border-{{ plan.color }}/10 h-full flex flex-col">
					{% if plan.badge %}
						<div class="absolute -top-7 sm:-top-8 -left-7 sm:-left-8 w-14 h-14 sm:w-16 sm:h-16 bg-white rounded-full shadow-lg flex items-center justify-center">
							<img src="{{ asset('images/chef-hat-orange.svg') }}" alt="Chef hat" class="w-8 h-8 sm:w-10 sm:h-10">
						</div>
					{% endif %}
					<h3 class="text-lg sm:text-xl font-bold font-second text-{{ plan.color }} mb-1">{{ plan.name }}</h3>
					<p class="mb-3 text-base sm:text-lg">{{ plan.price }}</p>
					<ul class="list-disc list-inside space-y-1 pt-2 flex-1">
						{% for f in plan.features %}
							<li>{{ f }}</li>
						{% endfor %}
					</ul>
					<div class="mt-4">
						<a href="/" class="inline-block text-center w-full rounded-md border border-{{ plan.color }} text-{{ plan.color }} hover:bg-{{ plan.color }}/10 transition px-4 py-2">
							Choisir cette formule
						</a>
					</div>
				</article>
			{% endfor %}
		</div>

		<div class="text-center">
			<a href="{{ path('app_restaurant_create') }}" class="btn text-base sm:text-lg px-6 sm:px-8 py-2.5 sm:py-3">Créer mon restaurant</a>
			<p class="mt-3 sm:mt-4 text-xs sm:text-sm">Commencez gratuitement. Sans engagement.</p>
		</div>
	</section>
{% endblock %}
